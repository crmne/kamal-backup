# Required for local file restores when production paths are configured.
# Paths are local targets in the same order as production paths.
databases:
  - name: app
    adapter: postgres
    url: postgres://localhost/chatwithwork_development
paths:
  - storage
state:
  path: tmp/kamal-backup
